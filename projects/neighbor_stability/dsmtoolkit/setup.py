from setuptools import setup

setup(name='dsmtoolkit',
      version='0.1',
      description='Functions to work with distributional semantic models',
      author='pia',
      packages=['dsmtoolkit'],
      zip_safe=False)
